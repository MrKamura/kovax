import rootPackage from "../../../../package.json";

/** Semver of `kovax-react` from the repo root `package.json` (source of truth for the playground UI). */
export const KOVAX_LIBRARY_VERSION: string = rootPackage.version;
