/** DOM attribute ThemeProvider sets on the theme target (`<html>` by default). */
export const KOVAX_THEME_ATTRIBUTE = "data-kovax-theme";

/** Default `localStorage` key — keep in sync with ThemeProvider `storageKey`. */
export const KOVAX_COLOR_MODE_STORAGE_KEY = "kovax-color-mode";
